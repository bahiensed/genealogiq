// The shared address form schema lives in @genealogiq/core (one source of truth
// for the address field set + defaults across BMS/SEQ/APP).
export { addressSchema, addressDefaultValues, type AddressFormValues } from "@genealogiq/core"
