// The Translator contract + identity translator are single-sourced in
// @genealogiq/core (shared by every form-schema factory across the 3 apps).
export { type Translator, identityTranslator } from "@genealogiq/core"
